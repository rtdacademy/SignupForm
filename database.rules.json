{
  "rules": {
    "adminEmails": {
      ".read": "auth != null && root.child('adminEmails').child(auth.token.email.replace('.', ',')).exists()",
      ".write": "auth != null && root.child('adminEmails').child(auth.token.email.replace('.', ',')).exists()"
    }
  }
}
